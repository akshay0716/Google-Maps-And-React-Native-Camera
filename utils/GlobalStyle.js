import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  CustomButtom: {
    marginTop: 20,
    borderWidth: 1,
    alignItems: 'center',
    padding: 10,
    borderRadius: 10,
    // color: 'white',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
